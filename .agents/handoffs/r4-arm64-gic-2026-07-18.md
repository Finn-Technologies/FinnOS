# Agent Handoff: R4.4 ARM64 GIC

- Objective: establish bounded, single-BSP GIC ownership for the supported QEMU `virt` profile and prove a real IRQ lifecycle before generic-timer or task-context work.
- Starting state: `f74fc49` on `main` with locally verified, uncommitted R4.1-R4.3 work stacked; `.freebuff/` is pre-existing and must remain untouched.
- Task state: locally complete on 2026-07-20; repository CI/PR integration pending.
- Scope: target-configured QEMU GIC version; owned page-table Device mappings; distributor/CPU-interface initialization with IRQs masked; current-EL IRQ vector dispatch; bounded acknowledge/validate/EOI/deactivate policy; isolated real-IRQ test; host model/adversarial validator tests; strict cross-Clippy, prior ARM modes, x86 regressions, CI, architecture/testing/status docs, and independent review.
- Non-goals: generic timer/timekeeping, device discovery abstraction, DT/ACPI parser, device drivers, MSI/ITS, GICv3 system-register interface, SMP/redistributors, nested IRQs, preemption, dynamic routing, EL0 delivery, physical hardware, or changing the shared trap ABI.
- Implemented contract: the target pins `virt,gic-version=2,secure=off` with one CPU. FinnOS maps GICD `0x08000000..0x0800ffff` and GICC `0x08010000..0x0801ffff` as Device RW/NX, uses the legacy single interrupt group exposed by that security-disabled model, and leaves DAIF.I masked in production after bounded initialization.
- Acceptance criteria:
  - Pure tests reject invalid distributor capacity, out-of-range IDs, spurious/reserved acknowledgements, wrong source CPU, unarmed/duplicate delivery, and invalid lifecycle transitions.
  - Production boot maps the bounded GICv2 MMIO range as Device RW/NX, initializes the distributor and CPU interface while DAIF.I remains masked, verifies controller identity/capacity/readback, and leaves IRQs masked without a consumer.
  - `test-arm64-gic --target arm64-qemu` arms one SGI, unmasks only IRQ, receives a current-EL SPx IRQ through the architectural vector, enters/exits the architecture-neutral interrupt-context guard, acknowledges the exact ID/source, writes EOI, remasks IRQ, verifies exactly one delivery/EOI and no pending/active test state, and exits semihosting status 0.
  - Exact-line validator rejects printed-marker spoofing, wrong/missing/duplicate/out-of-order evidence, inconsistent numeric controller/delivery/EOI values, cross-mode leakage, fatal diagnostics, and wrong QEMU status.
  - R4.1-R4.3 ARM modes, strict ARM cross-Clippy for production and feature modes, aggregate host checks, x86 first boot/page tables, formatting, governance/link validation, and diff hygiene remain green.
- Rollback: remove the isolated GIC module/feature/mode/vector branch/MMIO mapping, restore the target machine string, and remove its tooling/CI/docs without altering R4.1-R4.3.
- Local evidence: QEMU 11.0.2/AAVMF delivered SGI 1 through current-EL SPx IRQ source 5 with raw IAR token 1, one delivery, one exact-token EOI, spurious IDs 1023 before and after, restored interrupt depth 0, preserved x19 sentinel 1, restored DAIF `0x3c0`, and semihosting status 0. The exact-line validator also checks all fifteen numeric fields and rejects duplicates, reordering, spoofed markers, inconsistent lifecycle values, fatal output, cross-mode leakage, and wrong status.
- Important limitation: this validates a fixed QEMU GICv2/BSP lifecycle only. Security-state policy, DT/ACPI discovery, GICv3, SMP, external routing, generic timer delivery, and physical hardware remain unimplemented or unverified.
- Next action: integrate R4.1-R4.4 through the draft PR, then implement the ARM generic-timer slice without broadening the GIC claim.
